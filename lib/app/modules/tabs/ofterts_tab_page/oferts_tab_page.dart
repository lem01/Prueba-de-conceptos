import 'package:flutter/material.dart';

class OffertsTabPage extends StatelessWidget {
  const OffertsTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Oferts tab Page"),
      ),
    );
  }
}
